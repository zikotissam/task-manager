---
description: Seed the database with sample tasks for testing
---

Seed the database by running the seed script:

bash .opencode/scripts/seed-db.sh

This inserts 5-10 sample tasks with varying priorities and due dates into the SQLite database at `data/tasks.db`.
